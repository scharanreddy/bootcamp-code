"""Frontend view modules for ThreatLens AI.

Named ``views`` (not ``pages``) so Streamlit does not treat this directory as an
auto-discovered multipage app; navigation is handled explicitly in ``app.py``.
"""
